kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-claim-nfs
  namespace: system
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
