---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-readarr
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 15Gi
  storageClassName: ceph-filesystem
