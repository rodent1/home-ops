---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-overseerr-cache
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 15Gi
  storageClassName: ceph-block
