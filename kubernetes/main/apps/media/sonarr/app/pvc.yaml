---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-sonarr
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 15Gi
  storageClassName: ceph-filesystem
