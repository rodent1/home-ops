---
apiVersion: v1
kind: Service
metadata:
  name: jellyfin-ui
  namespace: networking
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8096
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: jellyfin-ui
  namespace: networking
  labels:
    kubernetes.io/service-name: jellyfin-ui
addressType: IPv4
ports:
  - name: http
    appProtocol: http
    protocol: TCP
    port: 8096
endpoints:
  - addresses:
      - "${JELLYFIN_IP}"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &app jellyfin-ui
  namespace: networking
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
  annotations:
    kubernetes.io/ingress.class: nginx
    external-dns.alpha.kubernetes.io/target: "ingress.${SECRET_DOMAIN}"
spec:
  rules:
    - host: jellyfin.${SECRET_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: *app
                port:
                  number: 80
